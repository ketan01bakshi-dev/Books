name: Validate graph schemas

on:
  pull_request:
    paths:
      - "schema/**"
      - "site/books/**/graph.json"
      - "site/subjects/**/master.json"
  push:
    branches: [main]
    paths:
      - "schema/**"
      - "site/books/**/graph.json"
      - "site/subjects/**/master.json"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r scripts/requirements.txt
      - run: python scripts/validate_graphs.py
